{
  "_id": "_design/package",
  "views": {
    "all": {
      "map": "function(doc) {
        if (doc.entityType == 'package') emit(null, doc);
      }"
    },
    "toproduct": {
      "map": "function(doc){
        if (doc.entityType == 'package'){
          emit(doc.productId, null);
        }
      }"
    }
  },
  "language": "javascript"
}
