{
  "type": "couchdb",
  "couchdb": {
    "host": "{COUCH_HOST}",
    "port": "{COUCH_PORT}",
    "db": "{COUCH_DB}",
    "filter": null,
    "script": "var docType = ctx.doc.docType; ctx._type = docType;"
  }
}
